using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StrawWindow : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    /// // set la range sur la balle ainsi que les degats
    //set l'enum pour dire quelle type de tir c'est dans la balle lors du tools
    //scale de la balle
    //spriterenderer
}
